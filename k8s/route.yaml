apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: gpu-sizer
  namespace: gpu-sizer
spec:
  host: gpu-sizer.apps.osmt.johan.ml
  port:
    targetPort: 3000
  tls:
    termination: edge
  to:
    kind: Service
    name: gpu-sizer
    weight: 100
  wildcardPolicy: None
